- name: demo on loops for pakages
  hosts: local
  connection: local
  tasks: 
    - name: install packages
      package:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - mysql