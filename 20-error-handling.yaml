- name: demo on cmd or shell
  hosts: local
  connection: local
  tasks:
  - name: demo on cmd or shell
    shell: cat /etc/passwd | grep root
    register: root_output

  - name: users result
    debug:
      msg: "Root User is avaialble or not {{ root_output }}"
       